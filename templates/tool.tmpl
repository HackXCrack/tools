{{define "tool"}}
<!DOCTYPE html>
<html>  
	<head>
		<title>Web-tools - {{.Title}}</title>
	</head>
	<body>
		{{template "header" .Title}}
		<hr>
		<form method="get">
			<input type="text" name="p">
			<input type="submit" value="{{.Title}}">
		</form>
		<p>{{.Request}}</p>
		{{template "footer"}}
	</body>
</html>
{{end}}
