{{define "tool"}}
<!DOCTYPE html>
<html>  
	<head>
		<title>Web-tools - {{.Title}}</title>
	</head>
	<body>
		{{template "header" &Page{Title:{{.Title}} } }}
		<form action="" method="GET">
			<div><input name="body" value="{{printf "%s" .Body}}"></div>
			<div><input type="submit" value="{{.Title}}"></div>
		</form>
		<h1>{{.R}}</h1>
	</body>
</html>
{{end}}
