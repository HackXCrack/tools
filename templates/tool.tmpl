{{define "tool"}}
<html>
<h2>{{.Title}}</h2>

<form action="" method="GET">
<div><input name="body" value="{{printf "%s" .Body}}"></input></div>
<div><input type="submit" value="{{.Title}}"></div>
</form>
<h1>{{.R}}</h1>
</html>
{{end}}
