{{define "ip"}}
<!DOCTYPE html>
<html>
	<head>
		<title>Web-tools - {{.Title}}</title>
		<link rel="stylesheet" href="/resources/style.css">
	</head>
	<body>
		  {{template "header" .Title}}
		  <p>{{.Request}}</p>
		  {{template "footer"}}
	  </body>
</html>
{{end}}
